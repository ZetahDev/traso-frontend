---
const features = [
  {
    icon: "👥",
    title: "Únete al Movimiento",
    description:
      "Afíliate a TRASO y sé parte de la transformación social. Proceso rápido y 100% digital.",
    link: "/afiliacion",
  },
  {
    icon: "🎓",
    title: "Formación Política",
    description:
      "Accede a nuestra Escuela de Formación con cursos gratuitos en liderazgo y participación ciudadana.",
    link: "/formacion",
  },
  {
    icon: "📅",
    title: "Eventos y Asambleas",
    description:
      "Participa en nuestras actividades, talleres y asambleas democráticas en todo el país.",
    link: "/eventos",
  },
  {
    icon: "📰",
    title: "Noticias y Comunicados",
    description:
      "Mantente informado sobre nuestras actividades, logros y propuestas políticas.",
    link: "/noticias",
  },
  {
    icon: "🌍",
    title: "Nuestro Movimiento",
    description:
      "Conoce nuestra historia, principios, estructura organizacional y visión de país.",
    link: "/nuestro-movimiento",
  },
  {
    icon: "🤝",
    title: "Participa y Propón",
    description:
      "Tu voz importa. Participa en consultas ciudadanas y construye propuestas desde tu territorio.",
    link: "#contacto",
  },
];
---

<section class="features" id="servicios">
  <div class="container">
    <div class="features-header">
      <h2>Nuestros Servicios</h2>
      <p>Herramientas diseñadas para fortalecer tu movimiento social</p>
    </div>

    <div class="features-grid">
      {
        features.map((feature) => (
          <a href={feature.link} class="feature-card">
            <div class="feature-icon">{feature.icon}</div>
            <h3>{feature.title}</h3>
            <p>{feature.description}</p>
            <span class="feature-arrow">→</span>
          </a>
        ))
      }
    </div>
  </div>
</section>

<style>
  .features {
    padding: 5rem 2rem;
    background: white;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .features-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .features-header h2 {
    font-size: 2.5rem;
    color: #1a1a2e;
    margin-bottom: 1rem;
  }

  .features-header p {
    font-size: 1.25rem;
    color: #4a5568;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2rem;
  }

  .feature-card {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.07);
    transition: all 0.3s;
    text-decoration: none;
    color: inherit;
    position: relative;
    overflow: hidden;
    border: 1px solid #e2e8f0;
  }

  .feature-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: #3b5998;
    transform: scaleX(0);
    transition: transform 0.3s;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 24px rgba(59, 89, 152, 0.2);
  }

  .feature-card:hover::before {
    transform: scaleX(1);
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    font-size: 1.5rem;
    color: #1a1a2e;
    margin-bottom: 0.75rem;
  }

  .feature-card p {
    color: #4a5568;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .feature-arrow {
    color: #3b5998;
    font-size: 1.5rem;
    font-weight: bold;
    opacity: 0;
    transition: opacity 0.3s;
  }

  .feature-card:hover .feature-arrow {
    opacity: 1;
  }

  @media (max-width: 768px) {
    .features {
      padding: 3rem 1.5rem;
    }

    .features-header h2 {
      font-size: 2rem;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
